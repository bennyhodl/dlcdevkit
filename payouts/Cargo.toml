[package]
name = "ddk-payouts"
version = "0.0.12"
authors = ["benny b <ben@bitcoinbay.foundation>"]
description = "Library to build payout functions for DLC contracts."
license = "MIT"
repository = "https://github.com/bennyhodl/dlcdevkit"
edition = "2021"

[dependencies]
ddk-dlc = { version = "0.7.0", features = ["use-serde"] }
ddk-manager = { version = "0.7.1", features = ["use-serde"] }
ddk-messages = { version = "0.7.0", features = ["use-serde"] }
ddk-trie = { version = "0.7.0", features = ["use-serde"] }
# ddk-dlc = { version = "0.7.0", path = "../../rust-dlc/ddk-dlc", features = ["use-serde"] }
# ddk-manager = { version = "0.7.1", path = "../../rust-dlc/ddk-manager", features = ["use-serde", "async"] }
# ddk-messages = { version = "0.7.0", path = "../../rust-dlc/ddk-messages", features = ["use-serde"] }
# ddk-trie = { version = "0.7.0", path = "../../rust-dlc/ddk-trie", features = ["use-serde"] }

bitcoin = "0.32.2"
serde = { version = "1.0.209", features = ["derive"] }
serde_json = "1.0.127"
anyhow = "1.0.86"
